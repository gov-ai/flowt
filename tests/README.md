To run tests, install `flowt` in env by running `pipenv install` in pipenv shell from main directory. Do not forget to add `__init__.py`
And then, run the below command from main dir using pipenv shell
```
pytest
```